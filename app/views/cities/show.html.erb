<h1><%= @city.name %></h1>

<%= image_tag @city.photo, class: 'show-photo' %><br>


<div class="flex-grid">
  <div class="user">
    <ul>
      <% @city.posts.each do |post| %>
      <li><%= post.author %></li>
      <% end %>
    </ul>
  </div>
  <div class="post">
    <ul>
      <% @city.posts.each do |post| %>
      	<li><%= link_to post.title, post_path(post.id) %></li>
      <% end %>
    </ul>
  </div>
</div>
