<div class="flex-grid">
  <div class="user">
    <img src="" alt="">
    <ul>
      <h4><%= @city.posts.user.name %></h4>
      <h4><%= @city.posts.user.created_at %></h4>
      <h4><%= @city.posts.user.current_city %></h4>
    </ul>
  </div>

  <div class="post">
    <%= @city.posts.title %><br>
    <%= link_to @post.author, user_path(@post.user_id) %><br>
    <%= @city.posts.content %>
  </div>
</div>
